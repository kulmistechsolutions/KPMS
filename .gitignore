# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Local secrets (use `.env.example` and `*.example` templates — do not commit real keys)
.env
.env.development
.env.production
# Edge-function secret runbooks — keep value-free templates only, never commit real values
supabase/*SECRETS.txt

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Release signing keystore (path from android/key.properties storeFile=../kpms-release-key.jks)
kpms-release-key.jks

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release
.vercel

node_modules/
.flutter-plugins
.packages
pubspec.lock